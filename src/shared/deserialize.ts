import { IDictionary } from "common-types";

/**
 * **deserialize**
 *
 * Deserializes a function and provides it with the "local context" passed in
 *
 * @param fn a string representation of a function and context serialized by the `serialize()` method
 */
export function deserialize<T = IDictionary>(fn: string) {
  // TODO: implement
}
