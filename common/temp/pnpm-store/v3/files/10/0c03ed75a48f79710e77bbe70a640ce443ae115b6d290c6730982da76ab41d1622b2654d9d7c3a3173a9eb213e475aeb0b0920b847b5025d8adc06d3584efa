import { CheckboxQuestion } from "inquirer";
import { Omit } from "common-types";
export declare function checkboxQuestion(q: Omit<CheckboxQuestion, "type">): {
    type: string;
    default?: any;
    message?: import("inquirer").AsyncDynamicQuestionProperty<string, import("inquirer").Answers>;
    name?: import("inquirer").KeyUnion<import("inquirer").Answers>;
    choices?: import("inquirer").AsyncDynamicQuestionProperty<readonly import("inquirer").DistinctChoice<import("inquirer").CheckboxChoiceMap<import("inquirer").Answers>>[], import("inquirer").Answers>;
    validate?: (input: any, answers?: import("inquirer").Answers) => string | boolean | Promise<string | boolean>;
    filter?: (input: any) => any;
    prefix?: string;
    suffix?: string;
    pageSize?: number;
    when?: import("inquirer").AsyncDynamicQuestionProperty<boolean, import("inquirer").Answers>;
};
