/**
 * Asks the user if they'd like to save information to their project or user
 * `do.config.js` file.
 */
export declare function askToSaveConfig(configPath: string, value: any, descriptor?: string): Promise<string>;
