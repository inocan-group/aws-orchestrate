/**
 * Uses the `chalk` utility to present a nicely formatted
 * filepath and file where the file name itself is highlighted
 */
export declare function highlightFilepath(fp: string, color?: string[], highlight?: string[]): string;
